// Ce fichier peut contenir des types personnalisés pour ThemeToggle si nécessaire
// Pour l'instant, le composant n'a pas de props personnalisées

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface ThemeToggleProps {
  children?: React.ReactNode;
}
